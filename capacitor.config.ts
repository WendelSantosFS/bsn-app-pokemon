import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'desafio-bsn-mobile-ionic',
  webDir: 'www'
};

export default config;
